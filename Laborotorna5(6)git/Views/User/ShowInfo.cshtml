@using Laborotorna5_6_.Controllers
@model List<ItemModel>

<h1>Інформація про товари</h1>

@if (Model.Count > 0)
{
    <ul>
    @foreach(var item in Model)
    {
        <li>
            @item.ToString();
        </li>
    }
    </ul>
}
<h4>@ViewBag.ErrorMessage</h4>

